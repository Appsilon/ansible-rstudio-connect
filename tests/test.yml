# test file
---
- name: Test
  hosts: localhost
  connection: local
  become: true
  pre_tasks:
    - name: Include vars
      ansible.builtin.include_vars: "{{ playbook_dir }}/vars/main.yml"
  roles:
    - ../../
